**Terraform Cloud - Repo Branching**

-   Example Using branches for each environment (Dev, Test, Prod)
-   Use variables to specify config differences (such as instance sizes, tags) between environments
